#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/home/ubuntu/synapse-browser/node_modules/.pnpm/wait-on@7.2.0_debug@4.4.3_supports-color@8.1.1__supports-color@8.1.1/node_modules/wait-on/node_modules:/home/ubuntu/synapse-browser/node_modules/.pnpm/wait-on@7.2.0_debug@4.4.3_supports-color@8.1.1__supports-color@8.1.1/node_modules:/home/ubuntu/synapse-browser/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="/home/ubuntu/synapse-browser/node_modules/.pnpm/wait-on@7.2.0_debug@4.4.3_supports-color@8.1.1__supports-color@8.1.1/node_modules/wait-on/node_modules:/home/ubuntu/synapse-browser/node_modules/.pnpm/wait-on@7.2.0_debug@4.4.3_supports-color@8.1.1__supports-color@8.1.1/node_modules:/home/ubuntu/synapse-browser/node_modules/.pnpm/node_modules:$NODE_PATH"
fi
if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../../bin/wait-on" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../bin/wait-on" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../../bin/wait-on" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../../bin/wait-on" "$@"
else
  exec node  "$basedir/../../bin/wait-on" "$@"
fi
# cmd-shim-target=/home/ubuntu/synapse-browser/node_modules/.pnpm/wait-on@7.2.0_debug@4.4.3_supports-color@8.1.1__supports-color@8.1.1/node_modules/wait-on/bin/wait-on
