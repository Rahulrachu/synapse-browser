import React, { useState, useEffect } from 'react';
import { Plus, X, Search, Check, Trash2, Save } from 'lucide-react';\nimport { useWorkspaceStore } from '../store/workspaceStore';\n\ninterface Note {\n  id: string;\n  title: string;\n  content: string;\n  createdAt: number;\n  updatedAt: number;\n}\n\ninterface TodoItem {\n  id: string;\n  text: string;\n  completed: boolean;\n  createdAt: number;\n}\n\ntype Tab = 'notes' | 'todo' | 'whiteboard' | 'search';\n\nexport default function ProductivityPanel() {\n  const isDarkMode = useWorkspaceStore((state) => state.isDarkMode);\n  const { notes, addNote, updateNote, deleteNote } = useWorkspaceStore();\n  const [activeTab, setActiveTab] = useState<Tab>('notes');\n  const [todos, setTodos] = useState<TodoItem[]>([]);\n  const [selectedNoteId, setSelectedNoteId] = useState<string | null>(null);\n  const [newNoteTitle, setNewNoteTitle] = useState('');\n  const [newTodoText, setNewTodoText] = useState('');\n  const [searchQuery, setSearchQuery] = useState('');\n  const [whiteboardContent, setWhiteboardContent] = useState('');\n\n  useEffect(() => {\n    loadTodos();\n  }, []);\n\n  const loadTodos = async () => {\n    try {\n      // Load from localStorage for now\n      const saved = localStorage.getItem('synapse-todos');\n      if (saved) {\n        setTodos(JSON.parse(saved));\n      }\n    } catch (error) {\n      console.error('Failed to load todos:', error);\n    }\n  };\n\n  const saveTodos = (newTodos: TodoItem[]) => {\n    setTodos(newTodos);\n    localStorage.setItem('synapse-todos', JSON.stringify(newTodos));\n  };\n\n  const handleAddNote = () => {\n    if (newNoteTitle.trim()) {\n      addNote(newNoteTitle, '');\n      setNewNoteTitle('');\n    }\n  };\n\n  const handleAddTodo = () => {\n    if (newTodoText.trim()) {\n      const newTodo: TodoItem = {\n        id: Date.now().toString(),\n        text: newTodoText,\n        completed: false,\n        createdAt: Date.now(),\n      };\n      saveTodos([...todos, newTodo]);\n      setNewTodoText('');\n    }\n  };\n\n  const handleToggleTodo = (todoId: string) => {\n    saveTodos(\n      todos.map(t =>\n        t.id === todoId ? { ...t, completed: !t.completed } : t\n      )\n    );\n  };\n\n  const handleDeleteTodo = (todoId: string) => {\n    saveTodos(todos.filter(t => t.id !== todoId));\n  };\n\n  const selectedNote = notes.find(n => n.id === selectedNoteId);\n  const filteredNotes = notes.filter(n =>\n    n.title.toLowerCase().includes(searchQuery.toLowerCase()) ||\n    n.content.toLowerCase().includes(searchQuery.toLowerCase())\n  );\n\n  return (\n    <div\n      className={`flex flex-col h-full rounded-lg border ${\n        isDarkMode ? 'border-gray-700 bg-synapse-darker' : 'border-gray-300 bg-white'\n      } overflow-hidden`}\n    >\n      {/* Header with Tabs */}\n      <div\n        className={`border-b ${\n          isDarkMode ? 'border-gray-700 bg-synapse-dark' : 'border-gray-200 bg-gray-50'\n        }`}\n      >\n        <div className=\"flex items-center gap-0\">\n          {(['notes', 'todo', 'whiteboard', 'search'] as Tab[]).map((tab) => (\n            <button\n              key={tab}\n              onClick={() => setActiveTab(tab)}\n              className={`flex-1 px-4 py-3 text-sm font-medium transition border-b-2 ${\n                activeTab === tab\n                  ? 'border-synapse-accent text-synapse-accent'\n                  : isDarkMode\n                  ? 'border-transparent text-gray-400 hover:text-gray-300'\n                  : 'border-transparent text-gray-600 hover:text-gray-900'\n              }`}\n            >\n              {tab.charAt(0).toUpperCase() + tab.slice(1)}\n            </button>\n          ))}\n        </div>\n      </div>\n\n      {/* Content */}\n      <div className=\"flex-1 overflow-hidden flex\">\n        {/* Notes Tab */}\n        {activeTab === 'notes' && (\n          <div className=\"flex-1 flex overflow-hidden\">\n            {/* Notes List */}\n            <div\n              className={`w-40 border-r ${isDarkMode ? 'border-gray-700 bg-gray-800' : 'border-gray-200 bg-gray-50'} overflow-y-auto p-2`}\n            >\n              <div className=\"mb-2\">\n                <input\n                  type=\"text\"\n                  value={newNoteTitle}\n                  onChange={(e) => setNewNoteTitle(e.target.value)}\n                  onKeyPress={(e) => e.key === 'Enter' && handleAddNote()}\n                  placeholder=\"New note...\"\n                  className={`w-full px-2 py-1 rounded border text-xs mb-2 ${\n                    isDarkMode\n                      ? 'bg-gray-700 border-gray-600 text-white placeholder-gray-400'\n                      : 'bg-white border-gray-300 text-gray-900 placeholder-gray-500'\n                  } focus:outline-none focus:ring-2 focus:ring-synapse-accent`}\n                />\n                <button\n                  onClick={handleAddNote}\n                  className=\"w-full px-2 py-1 bg-synapse-accent text-white rounded text-xs hover:bg-synapse-accent-light transition\"\n                >\n                  <Plus size={14} className=\"inline mr-1\" />\n                  Add\n                </button>\n              </div>\n\n              <div className=\"space-y-1\">\n                {filteredNotes.map((note) => (\n                  <button\n                    key={note.id}\n                    onClick={() => setSelectedNoteId(note.id)}\n                    className={`w-full text-left px-2 py-1 rounded text-xs truncate transition ${\n                      selectedNoteId === note.id\n                        ? 'bg-synapse-accent text-white'\n                        : isDarkMode\n                        ? 'bg-gray-700 hover:bg-gray-600 text-gray-300'\n                        : 'bg-gray-200 hover:bg-gray-300 text-gray-900'\n                    }`}\n                  >\n                    {note.title || 'Untitled'}\n                  </button>\n                ))}\n              </div>\n            </div>\n\n            {/* Note Editor */}\n            <div className=\"flex-1 flex flex-col overflow-hidden p-3\">\n              {selectedNote ? (\n                <>\n                  <input\n                    type=\"text\"\n                    value={selectedNote.title}\n                    onChange={(e) => updateNote(selectedNote.id, { title: e.target.value })}\n                    className={`px-2 py-1 rounded border mb-2 text-sm font-bold ${\n                      isDarkMode\n                        ? 'bg-gray-700 border-gray-600 text-white'\n                        : 'bg-white border-gray-300 text-gray-900'\n                    } focus:outline-none focus:ring-2 focus:ring-synapse-accent`}\n                  />\n                  <textarea\n                    value={selectedNote.content}\n                    onChange={(e) => updateNote(selectedNote.id, { content: e.target.value })}\n                    className={`flex-1 px-2 py-1 rounded border text-sm resize-none ${\n                      isDarkMode\n                        ? 'bg-gray-700 border-gray-600 text-white placeholder-gray-400'\n                        : 'bg-white border-gray-300 text-gray-900 placeholder-gray-500'\n                    } focus:outline-none focus:ring-2 focus:ring-synapse-accent`}\n                    placeholder=\"Start typing...\"\n                  />\n                  <div className=\"flex gap-2 mt-2\">\n                    <button\n                      onClick={() => deleteNote(selectedNote.id)}\n                      className=\"px-3 py-1 bg-red-600 text-white rounded text-xs hover:bg-red-700 transition\"\n                    >\n                      <Trash2 size={14} className=\"inline mr-1\" />\n                      Delete\n                    </button>\n                  </div>\n                </>\n              ) : (\n                <div className=\"flex items-center justify-center h-full\">\n                  <p className={isDarkMode ? 'text-gray-400' : 'text-gray-500'}>Select a note to edit</p>\n                </div>\n              )}\n            </div>\n          </div>\n        )}\n\n        {/* Todo Tab */}\n        {activeTab === 'todo' && (\n          <div className=\"flex-1 flex flex-col overflow-hidden p-3\">\n            <div className=\"mb-3 flex gap-2\">\n              <input\n                type=\"text\"\n                value={newTodoText}\n                onChange={(e) => setNewTodoText(e.target.value)}\n                onKeyPress={(e) => e.key === 'Enter' && handleAddTodo()}\n                placeholder=\"Add a task...\"\n                className={`flex-1 px-3 py-2 rounded border text-sm ${\n                  isDarkMode\n                    ? 'bg-gray-700 border-gray-600 text-white placeholder-gray-400'\n                    : 'bg-white border-gray-300 text-gray-900 placeholder-gray-500'\n                } focus:outline-none focus:ring-2 focus:ring-synapse-accent`}\n              />\n              <button\n                onClick={handleAddTodo}\n                className=\"px-3 py-2 bg-synapse-accent text-white rounded hover:bg-synapse-accent-light transition\"\n              >\n                <Plus size={18} />\n              </button>\n            </div>\n\n            <div className=\"flex-1 overflow-y-auto space-y-1\">\n              {todos.map((todo) => (\n                <div\n                  key={todo.id}\n                  className={`flex items-center gap-2 px-3 py-2 rounded transition ${\n                    isDarkMode ? 'bg-gray-800 hover:bg-gray-700' : 'bg-gray-100 hover:bg-gray-200'\n                  }`}\n                >\n                  <button\n                    onClick={() => handleToggleTodo(todo.id)}\n                    className={`flex-shrink-0 w-5 h-5 rounded border flex items-center justify-center transition ${\n                      todo.completed\n                        ? 'bg-synapse-accent border-synapse-accent'\n                        : isDarkMode\n                        ? 'border-gray-600 hover:border-synapse-accent'\n                        : 'border-gray-300 hover:border-synapse-accent'\n                    }`}\n                  >\n                    {todo.completed && <Check size={14} className=\"text-white\" />}\n                  </button>\n                  <span\n                    className={`flex-1 text-sm ${\n                      todo.completed\n                        ? isDarkMode\n                          ? 'text-gray-500 line-through'\n                          : 'text-gray-400 line-through'\n                        : isDarkMode\n                        ? 'text-gray-300'\n                        : 'text-gray-900'\n                    }`}\n                  >\n                    {todo.text}\n                  </span>\n                  <button\n                    onClick={() => handleDeleteTodo(todo.id)}\n                    className=\"flex-shrink-0 p-1 hover:bg-red-500 rounded transition\"\n                  >\n                    <X size={14} />\n                  </button>\n                </div>\n              ))}\n            </div>\n          </div>\n        )}\n\n        {/* Whiteboard Tab */}\n        {activeTab === 'whiteboard' && (\n          <div className=\"flex-1 flex flex-col overflow-hidden p-3\">\n            <textarea\n              value={whiteboardContent}\n              onChange={(e) => setWhiteboardContent(e.target.value)}\n              className={`flex-1 px-3 py-2 rounded border text-sm resize-none font-mono ${\n                isDarkMode\n                  ? 'bg-gray-700 border-gray-600 text-white placeholder-gray-400'\n                  : 'bg-white border-gray-300 text-gray-900 placeholder-gray-500'\n              } focus:outline-none focus:ring-2 focus:ring-synapse-accent`}\n              placeholder=\"Quick thoughts, sketches, and ideas...\"\n            />\n          </div>\n        )}\n\n        {/* Search Tab */}\n        {activeTab === 'search' && (\n          <div className=\"flex-1 flex flex-col overflow-hidden p-3\">\n            <input\n              type=\"text\"\n              value={searchQuery}\n              onChange={(e) => setSearchQuery(e.target.value)}\n              placeholder=\"Search notes...\"\n              className={`px-3 py-2 rounded border text-sm mb-3 ${\n                isDarkMode\n                  ? 'bg-gray-700 border-gray-600 text-white placeholder-gray-400'\n                  : 'bg-white border-gray-300 text-gray-900 placeholder-gray-500'\n              } focus:outline-none focus:ring-2 focus:ring-synapse-accent`}\n            />\n            <div className=\"flex-1 overflow-y-auto space-y-2\">\n              {filteredNotes.map((note) => (\n                <div\n                  key={note.id}\n                  onClick={() => {\n                    setSelectedNoteId(note.id);\n                    setActiveTab('notes');\n                  }}\n                  className={`p-2 rounded cursor-pointer transition ${\n                    isDarkMode ? 'bg-gray-800 hover:bg-gray-700' : 'bg-gray-100 hover:bg-gray-200'\n                  }`}\n                >\n                  <h4 className=\"font-semibold text-sm mb-1\">{note.title}</h4>\n                  <p className={`text-xs line-clamp-2 ${isDarkMode ? 'text-gray-400' : 'text-gray-600'}`}>\n                    {note.content}\n                  </p>\n                </div>\n              ))}\n            </div>\n          </div>\n        )}\n      </div>\n    </div>\n  );\n}\n
