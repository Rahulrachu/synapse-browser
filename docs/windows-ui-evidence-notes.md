# Native Windows UI Evidence Notes

The successful run `32934948618` produced real screenshots from the packaged Windows application through Windows UI Automation. `02-after-skip.png` shows the onboarding was dismissed and the normal Synapse Browser surface was visible: sidebar, Google tab, address bar, browser content, AI control, and READY status. `03-settings.png` shows Settings opened through the native accessibility automation and visibly contains browser preferences plus the AI provider section with provider selector, API-key field, base URL, model, Reset, and Save AI settings controls. The screenshots are evidence of launch, onboarding dismissal, normal surface, and Settings interaction; they do not by themselves prove successful provider authentication or full browser/workspace/persistence acceptance.
